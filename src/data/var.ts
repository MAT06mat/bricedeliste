export const enable_full_features = true;

export const bloc_sos_command =
    import.meta.env.VITE_DEV_MODE === "true" && false;

export const dev_mode = import.meta.env.VITE_DEV_MODE === "true";
